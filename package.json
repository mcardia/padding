{
  "name": "kwin-padding",
  "version": "1.0.0",
  "description": "KWin script that adds configurable padding around maximized and snapped windows",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
